(module
	;; Main file is generated by the bootstrap compiler
	(import "main" "main" (func $main))
	;; internals has the shared memory, every other module has to import from there
	(import "internals" "memory" (memory 0))

	(func (export "_start")
		(call $main)
	)
)
