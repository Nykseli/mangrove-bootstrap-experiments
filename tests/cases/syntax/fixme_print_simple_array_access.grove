// Output:
// 2

/* Fails since the array indexing is broken, only zero index works */

function main() -> none
{
	Array<Int32> foo = [1, 2, 3]
	Int32 bar = foo[1]
	__print_int(bar)
	__print_char('\n')
}
